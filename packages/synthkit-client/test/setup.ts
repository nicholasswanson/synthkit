// React-specific test setup
import { beforeAll } from 'vitest';

beforeAll(() => {
  // React-specific setup will go here when we add React component tests
  // For now, this file exists to ensure proper test infrastructure
});
